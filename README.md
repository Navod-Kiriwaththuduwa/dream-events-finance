# Dream Events Finance Platform V1.3.1 Hotfix

Fixes quotation generation where customer-visible Sub Items could still produce a zero quotation because legacy Main Items had been stored with `quotationVisible=false`.

Upload the contents of this folder over the existing repository files and commit to `main`.

Suggested commit message:

`Fix quotation visibility for existing budgets`
