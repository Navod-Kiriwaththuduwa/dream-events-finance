function listEvents_(user){ return getRows_('03_EVENTS').filter(r=>r.Status!=='ARCHIVED').map(r=>{const c=findOne_('02_CUSTOMERS','Customer_ID',r.Customer_ID);return {eventId:r.Event_ID,name:r.Event_Name,type:r.Event_Type,customerId:r.Customer_ID,customerName:c?c.Customer_Name:'',date:formatDateOnly_(r.Event_Date),venue:r.Venue,guestCount:Number(r.Guest_Count||0),status:r.Status,confirmedValue:user.Role==='FINANCE_HEAD'?Number(r.Confirmed_Value||0):null,notes:r.Notes,createdAt:r.Created_At};}); }
function createEvent_(user,data){ requireFinance_(user);requireFields_(data,['name','type','customerId','date']);const c=findOne_('02_CUSTOMERS','Customer_ID',data.customerId);if(!c)throw new Error('Customer not found.');const id=nextNumber_('EVENT','DE-EVT-',4);const rec={Event_ID:id,Event_Name:String(data.name).trim(),Event_Type:data.type,Customer_ID:data.customerId,Event_Date:data.date,Start_Time:data.startTime||'',End_Time:data.endTime||'',Venue:data.venue||'',Guest_Count:Number(data.guestCount||0),Coordinator:data.coordinator||'',Status:data.status||'INQUIRY',Package_ID:'',Quoted_Value:0,Confirmed_Value:Number(data.confirmedValue||0),Payment_Plan_Type:'',Notes:data.notes||'',Created_By:user.User_ID,Created_At:nowIso_(),Updated_By:user.User_ID,Updated_At:nowIso_()};appendObject_('03_EVENTS',rec);ensureEventFolder_(id);audit_(user,'CREATE_EVENT','EVENTS',id,null,rec,'',{});return {eventId:id,name:rec.Event_Name,type:rec.Event_Type,customerId:rec.Customer_ID,customerName:c.Customer_Name,date:data.date,venue:rec.Venue,status:rec.Status,confirmedValue:rec.Confirmed_Value}; }
function ensureEventFolder_(eventId){ const root=DriveApp.getFolderById(PropertiesService.getScriptProperties().getProperty(DE.DRIVE_PROP)); let eventsFolder=null;const it=root.getFoldersByName('Events');eventsFolder=it.hasNext()?it.next():root.createFolder('Events');const found=eventsFolder.getFoldersByName(eventId);if(found.hasNext())return found.next();const f=eventsFolder.createFolder(eventId);['Quotations','Invoices','Receipts','Supplier Bills','Expense Proof'].forEach(n=>f.createFolder(n));return f; }
function formatDateOnly_(v){ if(!v)return '';if(Object.prototype.toString.call(v)==='[object Date]')return Utilities.formatDate(v,'Asia/Colombo','yyyy-MM-dd');return String(v).slice(0,10); }
